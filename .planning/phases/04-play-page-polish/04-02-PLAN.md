---
phase: 04-play-page-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/routes/play/+page.svelte
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, API-04]
tags: [svelte, ui, error-handling, rate-limit, tv-layout]

must_haves:
  truths:
    - "No alert() call remains in src/routes/play/+page.svelte"
    - "No console.log('data', data) remains in src/routes/play/+page.svelte"
    - "When devices fetch in onMount returns data.requiresReauth, the page renders the orange re-auth banner with a 'Logg inn igjen' button — no automatic goto, no alert"
    - "When getNextSong receives error.requiresReauth (401), the page renders the same re-auth banner — no automatic goto, no alert"
    - "When playSong receives error.requiresReauth (401), the page renders the same re-auth banner — no automatic goto, no alert"
    - "When getNextSong receives HTTP 429, the page renders the yellow rate-limit banner with a 'PRØV IGJEN' button; clicking it clears rateLimited and retries getNextSong"
    - "The NESTE SANG button is disabled while requiresReauth is true (D-02)"
    - "Every async error-producing entry (getNextSong) clears requiresReauth, rateLimited, AND errorMessage at the top (D-01)"
    - "Only one banner / panel renders at a time, in priority order: requiresReauth > rateLimited > errorMessage > device selector"
    - "VIS SANG and NESTE SANG buttons render at text-lg md:text-xl px-10 py-6 (UI-04)"
    - "Songs-played counter renders at text-base hidden sm:block (UI-04)"
    - "The perspective-card CSS max-width is 480px (UI-04)"
  artifacts:
    - path: "src/routes/play/+page.svelte"
      provides: "Re-auth banner, 429 banner, generic error banner — all inline; TV-readable sizing"
      contains: "requiresReauth"
  key_links:
    - from: "src/routes/play/+page.svelte (getNextSong)"
      to: "/api/spotify/songs/random"
      via: "fetch + response.status === 429 check + error.requiresReauth check"
      pattern: "response\\.status === 429"
    - from: "src/routes/play/+page.svelte (template)"
      to: "$lib/components/ui/button Button"
      via: "Re-auth banner CTA + 429 PRØV IGJEN button"
      pattern: "variant=\"outline\""
    - from: "src/routes/play/+page.svelte (re-auth CTA)"
      to: "/auth/login/spotify"
      via: "goto('/auth/login/spotify') on user click only"
      pattern: "goto\\('/auth/login/spotify'\\)"
---

<objective>
Replace all three `alert()` + automatic `goto()` calls in `src/routes/play/+page.svelte` with two new `$state` flags (`requiresReauth`, `rateLimited`), an inline `{:else if}` priority chain that renders the appropriate banner, and a "PRØV IGJEN" recovery affordance for the 429 case. Remove the stray `console.log('data', data)` in `onMount`. Apply the TV-readable layout values (button text/padding, counter size, card max-width) from `04-UI-SPEC.md`.

Purpose: The party context demands inline UX — no browser dialogs, no surprise redirects. Three call sites currently call `alert()` + `goto()`; all three become `requiresReauth = true; return;` with banner-driven recovery. The 429 path is the only addition Plan 01 added on the server (HTTP 429 + `rateLimited: true`); this plan wires the client to that contract.

Output:
- New state flags: `requiresReauth = $state(false)`, `rateLimited = $state(false)`
- D-01 clearing extended at top of `getNextSong()`: clears all three flags
- D-02 button disable: `disabled={loading || requiresReauth}` on NESTE SANG
- 429 detection in `getNextSong()` before the `!response.ok` branch
- Three `alert() + goto()` blocks replaced with `requiresReauth = true; return;`
- Template `{:else if}` priority chain: `requiresReauth` → `rateLimited` → `errorMessage` → device selector
- `console.log('data', data)` removed (UI-03)
- TV-readable CSS values applied verbatim from UI-SPEC (UI-04)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-play-page-polish/04-CONTEXT.md
@.planning/phases/04-play-page-polish/04-RESEARCH.md
@.planning/phases/04-play-page-polish/04-PATTERNS.md
@.planning/phases/04-play-page-polish/04-UI-SPEC.md

@src/routes/play/+page.svelte

<interfaces>
<!-- Server contract consumed by this plan -->

The server route `/api/spotify/songs/random` (modified in Plan 04-01) returns on rate limit:
```
HTTP 429
{ "error": "Rate limited by Spotify", "rateLimited": true }
```

On Spotify auth failure (existing — unchanged):
```
HTTP 401
{ "error": "Spotify authentication failed", "requiresReauth": true }
```

`/api/spotify/devices` returns on auth failure (existing — unchanged):
```
HTTP 200 (or non-2xx)
{ "requiresReauth": true, ... }
```
The current `onMount` reads `data.requiresReauth` directly off the parsed JSON (200-level path).

`/api/spotify/player/play` returns on auth failure (existing — unchanged):
```
HTTP 401
{ "error": "...", "requiresReauth": true }
```

Existing imports in `play/+page.svelte` (do not redeclare): `Button` from `$lib/components/ui/button`, `goto` from `$app/navigation`, `onMount` from `svelte`, `parsePlaylistState`, `STORAGE_KEY` from `$lib/config/playlist-state`.

Existing $state variables in `play/+page.svelte` (do not redeclare; add the two new ones AFTER `errorMessage`): `currentTrack`, `isRevealed`, `loading`, `errorMessage`, `sessionId`, `selectedDefaults`, `customPlaylistUris`, `isPlaying`, `deviceId`, `availableDevices`, `showDeviceSelector`, `songsPlayed`, `clearPending`, `clearSuccess`.

`Track` and `SpotifyDevice` interfaces are declared locally inside the script block (lines 22–35) — leave untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add requiresReauth + rateLimited state, replace all 3 alert/goto sites, remove console.log, apply D-01 clearing and D-02 disable</name>
  <files>src/routes/play/+page.svelte</files>
  <read_first>
    - src/routes/play/+page.svelte (full current implementation — line numbers for alerts: 55–58 in onMount, 107–110 in getNextSong, 150–153 in playSong; console.log at line 61)
    - .planning/phases/04-play-page-polish/04-CONTEXT.md (D-01, D-02, state priority order in `<specifics>`)
    - .planning/phases/04-play-page-polish/04-RESEARCH.md ("Pattern 1: Svelte 5 $state", "Pattern 2: State priority in template", "Updated getNextSong() skeleton" example, "Pitfall 2: requiresReauth not cleared", "Pitfall 3: NESTE SANG button disabled incorrectly", "Pitfall 5: Device selector shown after requiresReauth banner")
    - .planning/phases/04-play-page-polish/04-PATTERNS.md ("src/routes/play/+page.svelte" section — every analog snippet for state, error-clear, alert replacement, !ok branch, disabled prop, console.log removal)
    - .planning/phases/04-play-page-polish/04-UI-SPEC.md ("Re-auth Inline Banner", "429 Rate Limit Banner", "State priority for the single display slot", "Copywriting Contract" Norwegian copy values)
  </read_first>
  <action>
    Edit `src/routes/play/+page.svelte`. All changes are in this single file. Do NOT touch other files.

    1. State declarations (script block, immediately after `let errorMessage = $state('');` at line 10):
       - Add `let requiresReauth = $state(false);`
       - Add `let rateLimited = $state(false);`
       Variable names are fixed by RESEARCH.md and CONTEXT.md (`requiresReauth`, `rateLimited` — camelCase, boolean).

    2. `onMount` block (around lines 55–58): replace the three-line block that calls `alert('Your Spotify session has expired. Please log in again.'); goto('/auth/login/spotify'); return;` with a two-line body: `requiresReauth = true; return;`. The `if (data.requiresReauth)` condition stays.

    3. Remove `console.log('data', data);` (line 61) entirely (UI-03). No replacement.

    4. `onMount` Norwegian copy fixes for inline error states already produced by this block (UI-SPEC "Copywriting Contract"):
       - Change `errorMessage = 'No Spotify devices found. Open Spotify on a device first.';` to `errorMessage = 'Ingen Spotify-enheter funnet. Åpne Spotify på en enhet først.';`
       - In the outer `catch`, change `errorMessage = 'Failed to connect to Spotify. Please try again.';` to `errorMessage = 'Klarte ikke å hente Spotify-enheter. Prøv igjen.';`

    5. `getNextSong()` (currently lines 87–130):
       - D-01 entry block: extend the existing `loading = true; errorMessage = ''; isRevealed = false;` to:
         `loading = true; errorMessage = ''; requiresReauth = false; rateLimited = false; isRevealed = false;`
         All three flags cleared BEFORE the try block. Per Pitfall 2 — this is the only place that resets these flags on a fresh user action.
       - Inside the `try` block, immediately after `const response = await fetch(...)` and BEFORE the existing `if (!response.ok)` branch, add a new 429 short-circuit:
         `if (response.status === 429) { rateLimited = true; return; }`
         Do not set `loading = false` here — the existing `finally { loading = false; }` already handles it.
       - In the existing `if (!response.ok) { ... }` block, replace the `if (error.requiresReauth) { alert(...); goto('/auth/login/spotify'); return; }` body with `if (error.requiresReauth) { requiresReauth = true; return; }`. Keep the surrounding `const error = await response.json();` and the trailing `errorMessage = error.error || ...;` lines.
       - Change the fallback Norwegian copy in the same `!ok` block from `'Failed to get next song'` to `'Kunne ikke laste neste sang. Prøv igjen.'`.
       - Change the outer `catch { errorMessage = 'Failed to load next song'; }` body to `errorMessage = 'Kunne ikke laste neste sang. Prøv igjen.';`.

    6. `playSong()` (currently lines 132–166):
       - In the `if (!playResponse.ok)` branch, replace `if (error.requiresReauth) { alert('Your Spotify session has expired. Please log in again.'); goto('/auth/login/spotify'); return; }` body with `if (error.requiresReauth) { requiresReauth = true; return; }`.
       - Change `errorMessage = \`Playback failed: ${error.error || 'Unknown error'}\`;` to `errorMessage = 'Avspilling feilet. Sjekk at Spotify er åpen på enheten.';` per UI-SPEC copywriting contract (drop the upstream error interpolation — single canonical Norwegian message).
       - Change the outer `catch` body fallback `errorMessage = 'Failed to start playback';` to `errorMessage = 'Avspilling feilet. Sjekk at Spotify er åpen på enheten.';`.
       - Do NOT clear `requiresReauth`/`rateLimited` at the top of `playSong()` — `playSong` is only ever invoked from `getNextSong`, which already cleared them.

    7. Device selector / error block (currently lines 275–300 template):
       Restructure the `{#if showDeviceSelector && availableDevices.length > 0} ... {:else if errorMessage} ... {:else if !currentTrack} ...` chain into a strict priority chain enforcing UI-SPEC State Priority Order. New chain shape (replace the existing block — keep the wrapper `<div class="text-center">`):
       ```
       {#if requiresReauth}
         <div class="bg-orange-500/10 border border-orange-500/50 rounded-lg p-4 space-y-3">
           <p class="text-base font-semibold">Spotify-økt utløpt</p>
           <p class="text-sm text-muted-foreground">Logg inn på nytt for å fortsette.</p>
           <Button variant="outline" onclick={() => goto('/auth/login/spotify')}>Logg inn igjen</Button>
         </div>
       {:else if rateLimited}
         <div class="bg-yellow-500/10 border border-yellow-500/50 rounded-lg p-4 space-y-3">
           <p class="text-base">Spotify er overbelastet. Vent litt og prøv igjen.</p>
           <Button variant="outline" size="sm" onclick={() => { rateLimited = false; getNextSong(); }}>PRØV IGJEN</Button>
         </div>
       {:else if showDeviceSelector && availableDevices.length > 0}
         <!-- existing device selector block unchanged -->
       {:else if errorMessage}
         <div class="p-4 md:p-6 rounded-lg bg-destructive/10 border border-destructive space-y-2">
           <p class="text-destructive text-base">{errorMessage}</p>
         </div>
       {:else if !currentTrack}
         <!-- existing "Klar til å spille!" block unchanged -->
       {/if}
       ```
       Note the position of the device selector: UI-SPEC lists priority as `requiresReauth > rateLimited > errorMessage > device selector`, but Pitfall 5 + the existing `onMount` flow shows the device selector is the initial UI surface (set `errorMessage` only when devices = 0). To avoid the "device selector shown next to re-auth banner" issue while keeping the "device selector visible until user picks one" behavior, place the device-selector branch AFTER `rateLimited` and BEFORE `errorMessage` — this matches the explicit UI-SPEC priority while still letting the device selector preempt the empty-devices `errorMessage`. Required ordering: `requiresReauth → rateLimited → showDeviceSelector → errorMessage → !currentTrack`.

       Bump the existing destructive error `<p>` from `class="text-destructive"` to `class="text-destructive text-base"` (UI-04: error banner body text minimum text-base at party distance).

    8. Button disable (D-02): on the NESTE SANG button (currently around line 396), change `disabled={loading}` to `disabled={loading || requiresReauth}`. Do NOT modify the VIS SANG button disable (per Pitfall 3 — D-02 mandates `requiresReauth` disabling only on NESTE SANG). Do NOT add `|| rateLimited` (per Pitfall 3 — the 429 banner has its own PRØV IGJEN affordance).

    9. TV layout adjustments (UI-04 — exact verbatim from UI-SPEC):
       - VIS SANG button (around line 387): change the class fragment `text-base md:text-lg px-8 py-6` to `text-lg md:text-xl px-10 py-6`. Keep all other classes (the linear-gradient fragment, hover, scale, font-bold) untouched.
       - NESTE SANG button (around line 396): same class swap — `text-base md:text-lg px-8 py-6` → `text-lg md:text-xl px-10 py-6`.
       - Songs counter (around line 253): change `class="text-sm text-muted-foreground hidden sm:block"` to `class="text-base text-muted-foreground hidden sm:block"`. (UI-04 explicitly bumps the desktop counter; do NOT touch the mobile counter at line ~409 — UI-SPEC table only references the desktop / `hidden sm:block` instance.)
       - `.perspective-card` CSS rule inside the `<style>` block (around line 433): change `max-width: 400px;` to `max-width: 480px;`. The selector and surrounding rules stay untouched.

    10. Do NOT add `Retry-After` parsing or countdown display (RESEARCH.md Open Question 2 — deferred; CONTEXT.md `<deferred>` confirms v2). Do NOT add automatic retry logic. Do NOT add a flash/toast component. Do NOT touch the flip-card markup, `togglePlayback`, `revealSong`, `clearHistory`, `endGame`, or the mobile-only bottom button block.

    Run `npx tsc --noEmit`, `npx prettier --check src/routes/play/+page.svelte`, and `npx eslint src/routes/play/+page.svelte` after editing.
  </action>
  <verify>
    <automated>grep -c "alert(" src/routes/play/+page.svelte</automated>
    <automated>grep -c "console.log" src/routes/play/+page.svelte</automated>
    <automated>grep -c "requiresReauth = \$state(false)" src/routes/play/+page.svelte</automated>
    <automated>grep -c "rateLimited = \$state(false)" src/routes/play/+page.svelte</automated>
    <automated>grep -c "disabled={loading || requiresReauth}" src/routes/play/+page.svelte</automated>
    <automated>grep -c "response.status === 429" src/routes/play/+page.svelte</automated>
    <automated>grep -c "Spotify-økt utløpt" src/routes/play/+page.svelte</automated>
    <automated>grep -c "Spotify er overbelastet" src/routes/play/+page.svelte</automated>
    <automated>grep -c "PRØV IGJEN" src/routes/play/+page.svelte</automated>
    <automated>grep -c "max-width: 480px" src/routes/play/+page.svelte</automated>
    <automated>grep -c "text-lg md:text-xl px-10 py-6" src/routes/play/+page.svelte</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx prettier --check src/routes/play/+page.svelte</automated>
    <human-check>Visit /play after running the dev server. (1) Force a 401 path by clearing the spotifyTokens row for your user — clicking NESTE SANG should render the orange "Spotify-økt utløpt" banner with a "Logg inn igjen" button and NO browser alert; clicking the button navigates to /auth/login/spotify. (2) Stub the songs/random response to status 429 (or rely on Plan 01's path if you can rate-limit) — banner becomes yellow with "PRØV IGJEN"; clicking it retries getNextSong. (3) On a desktop browser at >1.5m viewing distance, confirm the NESTE SANG button and "X sanger spilt" counter are readable.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "alert(" src/routes/play/+page.svelte` returns 0 (UI-01)
    - `grep -c "console.log" src/routes/play/+page.svelte` returns 0 (UI-03)
    - `grep -c "requiresReauth = \$state(false)" src/routes/play/+page.svelte` returns 1
    - `grep -c "rateLimited = \$state(false)" src/routes/play/+page.svelte` returns 1
    - `grep -c "requiresReauth = false" src/routes/play/+page.svelte` is at least 1 (D-01 clearing block in getNextSong)
    - `grep -c "rateLimited = false" src/routes/play/+page.svelte` is at least 2 (D-01 clearing + PRØV IGJEN handler)
    - `grep -c "requiresReauth = true" src/routes/play/+page.svelte` returns 3 (onMount, getNextSong !ok branch, playSong !ok branch — replaces the 3 alert sites)
    - `grep -c "response.status === 429" src/routes/play/+page.svelte` returns 1 (single 429 detection inside getNextSong)
    - `grep -c "disabled={loading || requiresReauth}" src/routes/play/+page.svelte` returns 1 (NESTE SANG only — D-02)
    - `grep -c "disabled={loading}" src/routes/play/+page.svelte` returns 0 (the only `disabled={loading}` was on NESTE SANG; VIS SANG never had `disabled`)
    - Re-auth banner copy present: `grep -c "Spotify-økt utløpt" src/routes/play/+page.svelte` returns 1; `grep -c "Logg inn på nytt for å fortsette." src/routes/play/+page.svelte` returns 1; `grep -c "Logg inn igjen" src/routes/play/+page.svelte` returns 1
    - 429 banner copy present: `grep -c "Spotify er overbelastet. Vent litt og prøv igjen." src/routes/play/+page.svelte` returns 1; `grep -c "PRØV IGJEN" src/routes/play/+page.svelte` returns 1
    - 429 banner CTA wires recovery: `grep -c "rateLimited = false; getNextSong()" src/routes/play/+page.svelte` returns 1
    - Re-auth banner CTA wires goto: `grep -c "goto('/auth/login/spotify')" src/routes/play/+page.svelte` returns exactly 1 (only the user-driven banner CTA — the 3 automatic gotos removed)
    - State priority chain present: file contains `{#if requiresReauth}` followed by `{:else if rateLimited}` followed by `{:else if showDeviceSelector && availableDevices.length > 0}` followed by `{:else if errorMessage}` followed by `{:else if !currentTrack}` in that template order. Verify with `grep -n '{#if requiresReauth}\|{:else if rateLimited}\|{:else if showDeviceSelector\|{:else if errorMessage}\|{:else if !currentTrack}' src/routes/play/+page.svelte` — line numbers strictly ascending in that order.
    - Banner surfaces present: `grep -c "bg-orange-500/10 border border-orange-500/50" src/routes/play/+page.svelte` returns 1; `grep -c "bg-yellow-500/10 border border-yellow-500/50" src/routes/play/+page.svelte` returns 1
    - TV layout values present:
      - `grep -c "text-lg md:text-xl px-10 py-6" src/routes/play/+page.svelte` returns 2 (VIS SANG + NESTE SANG)
      - `grep -c "text-base md:text-lg px-8 py-6" src/routes/play/+page.svelte` returns 0 (old class strings fully replaced)
      - `grep -c "text-base text-muted-foreground hidden sm:block" src/routes/play/+page.svelte` returns 1 (desktop counter)
      - `grep -c "max-width: 480px" src/routes/play/+page.svelte` returns 1
      - `grep -c "max-width: 400px" src/routes/play/+page.svelte` returns 0
    - Norwegian copy compliance: `grep -c "Kunne ikke laste neste sang. Prøv igjen." src/routes/play/+page.svelte` is at least 1; `grep -c "Avspilling feilet. Sjekk at Spotify er åpen på enheten." src/routes/play/+page.svelte` is at least 1; `grep -c "Ingen Spotify-enheter funnet. Åpne Spotify på en enhet først." src/routes/play/+page.svelte` returns 1; `grep -c "Klarte ikke å hente Spotify-enheter. Prøv igjen." src/routes/play/+page.svelte` returns 1
    - English copy fully removed: `grep -c "Your Spotify session has expired" src/routes/play/+page.svelte` returns 0; `grep -c "Failed to get next song" src/routes/play/+page.svelte` returns 0; `grep -c "Failed to load next song" src/routes/play/+page.svelte` returns 0; `grep -c "Failed to start playback" src/routes/play/+page.svelte` returns 0; `grep -c "Playback failed:" src/routes/play/+page.svelte` returns 0
    - `npx tsc --noEmit` exits 0
    - `npx prettier --check src/routes/play/+page.svelte` exits 0
    - `npx eslint src/routes/play/+page.svelte` exits 0
    - Behavior (manual): re-auth banner appears on 401; user must click button to navigate; rate-limit banner appears on 429; clicking PRØV IGJEN retries (no auto-retry); NESTE SANG is disabled while requiresReauth is true; counter and CTA buttons are visibly larger than before
  </acceptance_criteria>
  <done>All three alert/goto sites converted to requiresReauth = true; 429 handled inline with PRØV IGJEN; console.log gone; D-01 clears all three flags at getNextSong entry; D-02 NESTE SANG disable in place; TV layout values applied verbatim from UI-SPEC; TypeScript / Prettier / ESLint all clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser play page → /api/spotify/* | Existing fetches; no new endpoints. Trust boundary unchanged. |
| user → re-auth banner CTA | User-initiated click triggers `goto('/auth/login/spotify')` (existing route) |
| /api/spotify/songs/random → play page | New 429 + rateLimited response field consumed (contract added in Plan 04-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | Banner click-through to /auth/login/spotify | accept | Existing OAuth route — banner only triggers navigation on user click; no new auth logic introduced |
| T-04-02-02 | Tampering | requiresReauth / rateLimited client state | accept | Pure UI state; tampering only affects which banner renders for the local user. The server still authoritatively returns 401 or 429 — flag flipped to true via devtools cannot grant access |
| T-04-02-03 | Information Disclosure | playSong upstream error string | mitigate | Playback error stops interpolating upstream `error.error` string into `errorMessage` (current code: `\`Playback failed: ${error.error}\``). New copy is a canonical Norwegian message — no chance of leaking Spotify-side detail strings to the UI |
| T-04-02-04 | Denial of Service | PRØV IGJEN click-spam against 429 endpoint | accept | User-paced clicks cannot exceed network round-trip rate; Spotify's own rate-limit will continue to return 429 → banner re-renders. No retry loop, no setTimeout |
| T-04-02-05 | Repudiation | console.log removal hiding diagnostic data | accept | Removed line was `console.log('data', data)` on raw device payload — never used for audit; production builds strip dev logs anyway |
| T-04-02-SC | Tampering | npm package installs | accept | No new packages installed by this plan. No package legitimacy audit required. |
</threat_model>

<verification>
- After Task 1: every `grep` in `<acceptance_criteria>` matches; `npx tsc --noEmit`, `npx prettier --check src/routes/play/+page.svelte`, and `npx eslint src/routes/play/+page.svelte` all exit 0.
- Phase-level: only `src/routes/play/+page.svelte` is in `git diff --stat` output for this plan.
</verification>

<success_criteria>
- UI-01 satisfied: `grep -c "alert(" src/routes/play/+page.svelte` returns 0
- UI-02 satisfied: re-auth banner rendered inline; 3 `requiresReauth = true` assignments replace the 3 removed `alert() + goto()` sites; only 1 `goto('/auth/login/spotify')` remains (the user-click CTA)
- UI-03 satisfied: `grep -c "console.log" src/routes/play/+page.svelte` returns 0
- UI-04 satisfied: all four CSS value swaps applied; counter and CTAs visibly larger; card width bumped to 480px
- API-04 (client portion) satisfied: HTTP 429 from songs/random sets `rateLimited = true`, no auto-retry, manual PRØV IGJEN clears the flag and retries
</success_criteria>

<output>
Create `.planning/phases/04-play-page-polish/04-02-SUMMARY.md` when done
</output>
