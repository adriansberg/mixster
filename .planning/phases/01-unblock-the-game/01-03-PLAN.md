---
phase: 01-unblock-the-game
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/spotify.ts
  - src/routes/api/spotify/devices/+server.ts
  - src/routes/api/spotify/player/play/+server.ts
  - src/routes/api/auth/reauth/+server.ts
  - src/lib/types/spotify-player.d.ts
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "When Spotify tokens expire, the app detects it and propagates SpotifyAuthError to the route boundary"
    - "POST /api/auth/reauth clears all sessions and Spotify tokens for the authenticated user"
    - "SpotifyPlayerService type definitions are in src/lib/types/spotify-player.d.ts"
  artifacts:
    - path: "src/lib/server/spotify.ts"
      provides: "SpotifyAuthError class, deleteSpotifyTokens, parseSpotifyPlaylistId — all committed"
      exports: ["spotifyFetch", "SpotifyAuthError", "deleteSpotifyTokens", "parseSpotifyPlaylistId", "getSpotifyAccessToken"]
    - path: "src/routes/api/auth/reauth/+server.ts"
      provides: "POST /api/auth/reauth — session + token invalidation"
      exports: ["POST"]
    - path: "src/lib/types/spotify-player.d.ts"
      provides: "Spotify SDK type declarations"
  key_links:
    - from: "spotifyFetch"
      to: "SpotifyAuthError"
      via: "thrown on 401 response or missing token"
      pattern: "throw new SpotifyAuthError"
    - from: "POST /api/auth/reauth"
      to: "deleteSpotifyTokens + invalidateUserSessions"
      via: "sequential await calls"
      pattern: "deleteSpotifyTokens"
---

<objective>
Commit the five AUTH-01 files that are complete in the working tree. These implement the
SpotifyAuthError re-auth infrastructure (class definition, token cleanup, reauth endpoint,
SpotifyAuthError catches in devices and player/play routes, and Spotify SDK type declarations).

Purpose: These changes are already written and tested — they just need to be committed. The
reauth flow is required for the game to recover from expired tokens.

Output: All five files committed. SpotifyAuthError pattern is in place across the codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-unblock-the-game/01-CONTEXT.md

<interfaces>
<!-- AUTH-01 files are complete. Executor verifies content, then commits. -->

src/lib/server/spotify.ts exports:
  SpotifyAuthError (class), deleteSpotifyTokens(userId), parseSpotifyPlaylistId(input),
  spotifyFetch<T>(userId, endpoint, options), getSpotifyAccessToken(userId)

src/routes/api/auth/reauth/+server.ts:
  POST handler — requires locals.user, calls invalidateUserSessions + deleteSpotifyTokens +
  deleteSessionTokenCookie, returns { success: true }

src/routes/api/spotify/devices/+server.ts:
  Catches SpotifyAuthError → { requiresReauth: true, status: 401 }

src/routes/api/spotify/player/play/+server.ts:
  Catches SpotifyAuthError → { requiresReauth: true, status: 401 }

src/lib/types/spotify-player.d.ts:
  Global Window interface extension + Spotify namespace + SpotifyPlayer/SpotifyTrack interfaces
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify AUTH-01 files are complete and commit them</name>
  <files>
    src/lib/server/spotify.ts,
    src/routes/api/spotify/devices/+server.ts,
    src/routes/api/spotify/player/play/+server.ts,
    src/routes/api/auth/reauth/+server.ts,
    src/lib/types/spotify-player.d.ts
  </files>
  <read_first>
    - src/lib/server/spotify.ts (verify SpotifyAuthError class and deleteSpotifyTokens exist)
    - src/routes/api/spotify/devices/+server.ts (verify SpotifyAuthError catch present)
    - src/routes/api/spotify/player/play/+server.ts (verify SpotifyAuthError catch present)
    - src/routes/api/auth/reauth/+server.ts (verify POST handler with invalidateUserSessions + deleteSpotifyTokens)
    - src/lib/types/spotify-player.d.ts (verify file exists and has Window interface extension)
  </read_first>
  <action>
    Read each of the five files listed above. Verify each contains the expected AUTH-01 content:

    - spotify.ts: exports SpotifyAuthError class with name 'SpotifyAuthError', exports deleteSpotifyTokens(userId), exports parseSpotifyPlaylistId(input)
    - devices/+server.ts: catch block contains `if (error instanceof SpotifyAuthError)` returning { requiresReauth: true } with status 401
    - player/play/+server.ts: same SpotifyAuthError catch pattern
    - reauth/+server.ts: POST handler exists, calls invalidateUserSessions, deleteSpotifyTokens, deleteSessionTokenCookie
    - spotify-player.d.ts: file exists with Window interface extension or Spotify namespace

    If any file is missing expected content, note it in the summary and do NOT commit — flag as a blocker.

    If all files are as expected, run: npx tsc --noEmit to confirm TypeScript is clean.

    Then stage and commit only these five files:
    git add src/lib/server/spotify.ts src/routes/api/spotify/devices/+server.ts src/routes/api/spotify/player/play/+server.ts src/routes/api/auth/reauth/+server.ts src/lib/types/spotify-player.d.ts

    Commit message: feat(auth): add SpotifyAuthError re-auth infrastructure (AUTH-01)
  </action>
  <verify>
    <automated>cd /Users/asb/Projects/priv/shitster && git log --oneline -3 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - git log shows a commit with message containing 'SpotifyAuthError' or 'AUTH-01'
    - git status shows src/lib/server/spotify.ts, src/routes/api/spotify/devices/+server.ts, src/routes/api/spotify/player/play/+server.ts, src/routes/api/auth/reauth/+server.ts, src/lib/types/spotify-player.d.ts are no longer in the modified/untracked list
    - npx tsc --noEmit exits 0
    - grep -c 'SpotifyAuthError' src/lib/server/spotify.ts returns >= 1
    - grep -c 'requiresReauth' src/routes/api/spotify/devices/+server.ts returns >= 1
  </acceptance_criteria>
  <done>
    All five AUTH-01 files committed. SpotifyAuthError is in the codebase history. TypeScript compiles clean. git status shows these files are clean (not modified).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/auth/reauth | Authenticated endpoint — requires valid session cookie |
| server → Spotify API | Token invalidation path — no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | POST /api/auth/reauth without auth | mitigate | Handler guards `if (!locals.user)` returning 401 — unauthenticated callers cannot trigger session invalidation |
| T-03-02 | Denial of Service | POST /api/auth/reauth by authenticated user | accept | User can only invalidate their own session — no cross-user impact; self-DoS is acceptable |
| T-03-03 | Information Disclosure | SpotifyAuthError message | accept | Error message is 'Re-authentication required' — no token value or user data in message |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase installs no new packages |
</threat_model>

<verification>
Run after plan execution:
1. `npx tsc --noEmit` — must exit 0
2. `git log --oneline -5` — must show AUTH-01 commit
3. `git status` — the five AUTH-01 files must not appear as modified or untracked
4. `grep -c 'SpotifyAuthError' src/lib/server/spotify.ts` — must be >= 2 (class def + throw)
5. `test -f src/routes/api/auth/reauth/+server.ts && echo exists` — must print 'exists'
</verification>

<success_criteria>
All five AUTH-01 files committed to git. SpotifyAuthError class and deleteSpotifyTokens
exported from spotify.ts. reauth endpoint live at POST /api/auth/reauth. TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/01-unblock-the-game/01-03-SUMMARY.md` when done.
</output>
