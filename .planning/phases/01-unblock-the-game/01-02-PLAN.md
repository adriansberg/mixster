---
phase: 01-unblock-the-game
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api/spotify/playlist/validate/+server.ts
  - src/routes/api/spotify/playlists/track-counts/+server.ts
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - "Playlist metadata (name, track count) resolves from a Spotify URL or URI"
    - "track-counts returns correct totals for all playlists passed to it"
  artifacts:
    - path: "src/routes/api/spotify/playlist/validate/+server.ts"
      provides: "POST /api/spotify/playlist/validate — items.total field, surgical change"
      exports: ["POST"]
    - path: "src/routes/api/spotify/playlists/track-counts/+server.ts"
      provides: "POST /api/spotify/playlists/track-counts — items.total field + parseSpotifyPlaylistId"
      exports: ["POST"]
  key_links:
    - from: "validate handler"
      to: "GET /playlists/{id}?fields=id,name,uri,items.total"
      via: "spotifyFetch<{ id, name, uri, items: { total } }>"
      pattern: "items\\.total"
    - from: "track-counts handler"
      to: "GET /playlists/{id}?fields=items.total"
      via: "spotifyFetch<{ items: { total } }>"
      pattern: "items\\.total"
---

<objective>
Fix the tracks.total → items.total field rename in the two playlist metadata routes (D-04).
Also fix the positional URI split in track-counts (uses .split(':').pop() instead of parseSpotifyPlaylistId).

Purpose: After the Feb 2026 Spotify API change, the tracks.total field is no longer returned
for playlists the user does not own. The items.total field replaces it on GET /playlists/{id}.

Output: Both routes use items.total in fields query, TypeScript type, and accessor. track-counts
uses parseSpotifyPlaylistId instead of positional split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unblock-the-game/01-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/lib/server/spotify.ts:
```typescript
export async function spotifyFetch<T = unknown>(userId: string, endpoint: string, options?: RequestInit): Promise<T | null>
export function parseSpotifyPlaylistId(input: string): string | null
```

validate response contract (unchanged — consumer is setup page):
```typescript
// Returns:
{ name: string; uri: string; trackCount: number }
// trackCount comes from playlist.items?.total || 0
```

track-counts response contract (unchanged):
```typescript
// Returns:
{ trackCounts: Record<string, number> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validate endpoint — tracks.total → items.total (D-04)</name>
  <files>src/routes/api/spotify/playlist/validate/+server.ts</files>
  <read_first>
    - src/routes/api/spotify/playlist/validate/+server.ts (read fully — CLAUDE.md flags this as "live caller")
    - .planning/phases/01-unblock-the-game/01-CONTEXT.md (D-04 decision)
    - .planning/phases/01-unblock-the-game/01-PATTERNS.md (validate section with exact before/after)
  </read_first>
  <action>
    Make three surgical changes to src/routes/api/spotify/playlist/validate/+server.ts:

    1. Fields param on line ~37: change `?fields=name,id,uri,tracks.total` to `?fields=id,name,uri,items.total`

    2. TypeScript generic on spotifyFetch call (lines ~30-35): change the inline type from
       `{ name: string; id: string; uri: string; tracks: { total: number } }`
       to
       `{ id: string; name: string; uri: string; items: { total: number } }`

    3. Accessor on line ~50: change `playlist.tracks?.total || 0` to `playlist.items?.total || 0`

    Nothing else changes — imports, auth guard, error handling, response shape, and the parseSpotifyPlaylistId call at line 20 are all untouched.
  </action>
  <verify>
    <automated>cd /Users/asb/Projects/priv/mixster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/api/spotify/playlist/validate/+server.ts contains 'items.total' in the fields string
    - src/routes/api/spotify/playlist/validate/+server.ts contains 'items: { total: number }' in the TypeScript type
    - src/routes/api/spotify/playlist/validate/+server.ts contains 'playlist.items?.total' as the accessor
    - src/routes/api/spotify/playlist/validate/+server.ts does NOT contain 'tracks.total' or 'tracks: { total'
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    POST /api/spotify/playlist/validate returns { name, uri, trackCount } where trackCount reads from items.total. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix track-counts endpoint — tracks.total → items.total + parseSpotifyPlaylistId (D-04, D-02)</name>
  <files>src/routes/api/spotify/playlists/track-counts/+server.ts</files>
  <read_first>
    - src/routes/api/spotify/playlists/track-counts/+server.ts (read fully)
    - .planning/phases/01-unblock-the-game/01-PATTERNS.md (track-counts section with exact before/after)
  </read_first>
  <action>
    Make four changes to src/routes/api/spotify/playlists/track-counts/+server.ts:

    1. Import: add parseSpotifyPlaylistId to the import from '$lib/server/spotify' (currently only spotifyFetch is imported).

    2. URI parsing (line ~25): replace `const playlistId = uri.split(':').pop();` with `const playlistId = parseSpotifyPlaylistId(uri);`. Keep the existing null guard `if (!playlistId) return;` — it still applies.

    3. spotifyFetch TypeScript generic (line ~28): change `{ tracks: { total: number } }` to `{ items: { total: number } }`.

    4. Fields param (line ~30): change `?fields=tracks.total` to `?fields=items.total`.

    5. Accessor (line ~34): change `data.tracks?.total || 0` to `data.items?.total || 0`.

    Nothing else changes — auth guard, request parsing, Promise.all, error handling, and response shape are all untouched.
  </action>
  <verify>
    <automated>cd /Users/asb/Projects/priv/mixster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/api/spotify/playlists/track-counts/+server.ts contains 'parseSpotifyPlaylistId' in the import line
    - src/routes/api/spotify/playlists/track-counts/+server.ts contains 'items.total' in the fields string
    - src/routes/api/spotify/playlists/track-counts/+server.ts contains 'items: { total: number }' in the TypeScript type
    - src/routes/api/spotify/playlists/track-counts/+server.ts contains 'data.items?.total' as the accessor
    - src/routes/api/spotify/playlists/track-counts/+server.ts does NOT contain 'tracks.total' or 'tracks: { total'
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    POST /api/spotify/playlists/track-counts returns { trackCounts: Record<string, number> } reading from items.total. Uses parseSpotifyPlaylistId for URI parsing. TypeScript compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/spotify/playlist/validate | playlistInput (URL/URI string) from browser — untrusted |
| client → POST /api/spotify/playlists/track-counts | playlistUris array from browser — untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | playlistInput in validate | mitigate | parseSpotifyPlaylistId validates format before use; invalid input returns 400 |
| T-02-02 | Tampering | playlistUris array in track-counts | accept | Each URI parsed via parseSpotifyPlaylistId; invalid URIs skipped (continue); no injection vector |
| T-02-03 | Information Disclosure | Spotify playlist metadata | accept | Returns only name, uri, trackCount — no tokens or user data leaked |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase installs no new packages |
</threat_model>

<verification>
Run after plan execution:
1. `npx tsc --noEmit` — must exit 0
2. `grep -c 'items\.total' src/routes/api/spotify/playlist/validate/+server.ts` — must be >= 2 (fields + accessor)
3. `grep -c 'items\.total' src/routes/api/spotify/playlists/track-counts/+server.ts` — must be >= 2 (fields + accessor)
4. `grep -c 'tracks\.total' src/routes/api/spotify/playlist/validate/+server.ts` — must be 0
5. `grep -c 'tracks\.total' src/routes/api/spotify/playlists/track-counts/+server.ts` — must be 0
</verification>

<success_criteria>
Both routes:
- Use items.total in fields param, TypeScript generic, and accessor (D-04)
- track-counts uses parseSpotifyPlaylistId (D-02)
- TypeScript compiles clean
- Client-facing response shape unchanged
</success_criteria>

<output>
Create `.planning/phases/01-unblock-the-game/01-02-SUMMARY.md` when done.
</output>
