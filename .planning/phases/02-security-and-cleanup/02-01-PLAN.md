---
phase: 02-security-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api/spotify/token/+server.ts
  - src/routes/api/spotify/player/start/+server.ts
  - src/routes/api/spotify/playlists/+server.ts
  - src/routes/api/spotify/playlist/add/+server.ts
  - src/routes/api/spotify/playlist/toggle/+server.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "Raw Spotify access token is no longer reachable by browser via /api/spotify/token"
    - "IDOR-vulnerable playlist DELETE and PATCH routes no longer exist"
    - "Duplicate player/start route no longer exists"
    - "TypeScript still compiles after deletions (ignoring email.ts errors handled in plan 02)"
  artifacts:
    - path: "src/routes/api/spotify/token/+server.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/routes/api/spotify/player/start/+server.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/routes/api/spotify/playlists/+server.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/routes/api/spotify/playlist/add/+server.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/routes/api/spotify/playlist/toggle/+server.ts"
      provides: "ABSENT — file must not exist"
  key_links:
    - from: "src/routes/play/+page.svelte"
      to: "/api/spotify/player/play"
      via: "fetch calls in play page"
      pattern: "player/play"
    - from: "src/routes/play/+page.svelte"
      to: "/api/spotify/songs/random"
      via: "fetch calls in play page"
      pattern: "songs/random"
---

<objective>
Delete five dead or dangerous API route files: the raw token endpoint (SEC-01), three IDOR-vulnerable/dead playlist routes (SEC-02), and the duplicate player start route (SEC-03).

Purpose: Eliminates a token exfiltration surface and two IDOR vulnerabilities. No caller in the active codebase references any of these routes.

Output: Five files deleted, TypeScript still compiles (email.ts errors are pre-existing and addressed in plan 02, not introduced by these deletions).
</objective>

<execution_context>
@/Users/asb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Active routes NOT to touch (verified callers in play page):
- src/routes/api/spotify/devices/+server.ts
- src/routes/api/spotify/songs/random/+server.ts
- src/routes/api/spotify/player/play/+server.ts
- src/routes/api/spotify/player/pause/+server.ts
- src/routes/api/spotify/playlists/track-counts/+server.ts
- src/routes/api/spotify/playlist/validate/+server.ts
- src/routes/api/auth/ (all)
- src/hooks.server.ts

Dead routes being deleted (no callers):
- token/+server.ts — exposes getSpotifyAccessToken() result to browser
- player/start/+server.ts — PUT, duplicate of player/play which is the actual caller target
- playlists/+server.ts — GET+DELETE on dead userPlaylists table; DELETE lacks userId check (IDOR)
- playlist/add/+server.ts — POST writes to dead userPlaylists table
- playlist/toggle/+server.ts — PATCH on userPlaylists; lacks userId check (IDOR)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead and dangerous route files</name>
  <files>
    src/routes/api/spotify/token/+server.ts
    src/routes/api/spotify/player/start/+server.ts
    src/routes/api/spotify/playlists/+server.ts
    src/routes/api/spotify/playlist/add/+server.ts
    src/routes/api/spotify/playlist/toggle/+server.ts
  </files>
  <action>
    Delete all five files using git rm so the deletion is staged for commit.

    Commands:
      git rm src/routes/api/spotify/token/+server.ts
      git rm src/routes/api/spotify/player/start/+server.ts
      git rm src/routes/api/spotify/playlists/+server.ts
      git rm src/routes/api/spotify/playlist/add/+server.ts
      git rm src/routes/api/spotify/playlist/toggle/+server.ts

    Do NOT touch:
    - src/routes/api/spotify/playlists/track-counts/+server.ts (active, keep)
    - src/routes/api/spotify/playlist/validate/+server.ts (active, keep)
    - src/routes/api/spotify/player/play/+server.ts (active, keep)

    After deletion, run tsc to confirm no NEW errors were introduced. Pre-existing email.ts errors
    (RESEND_API_KEY, EMAIL_FROM) are expected and will be fixed in plan 02 — do not treat them
    as a blocker for this plan.
  </action>
  <verify>
    <automated>
      test ! -f src/routes/api/spotify/token/+server.ts &amp;&amp;
      test ! -f src/routes/api/spotify/player/start/+server.ts &amp;&amp;
      test ! -f src/routes/api/spotify/playlists/+server.ts &amp;&amp;
      test ! -f src/routes/api/spotify/playlist/add/+server.ts &amp;&amp;
      test ! -f src/routes/api/spotify/playlist/toggle/+server.ts &amp;&amp;
      echo "All five files absent — OK"
    </automated>
    <automated>
      test -f src/routes/api/spotify/playlists/track-counts/+server.ts &amp;&amp;
      test -f src/routes/api/spotify/playlist/validate/+server.ts &amp;&amp;
      echo "Active routes preserved — OK"
    </automated>
    <automated>
      npx tsc --noEmit 2>&amp;1 | grep -v 'email\.ts' | grep -v 'RESEND_API_KEY' | grep -v 'EMAIL_FROM' | grep 'error TS' | head -20 || echo "No new TS errors from route deletions — OK"
    </automated>
  </verify>
  <done>
    All five files absent on disk. Active routes (track-counts, validate, player/play) still present.
    No TypeScript errors introduced by these deletions (email.ts pre-existing errors are acceptable).
    Changes staged via git rm.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Unauthenticated browser requests reaching Spotify token values |
| userId→resource | API routes operating on DB rows without verifying row ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | token/+server.ts | mitigate | Delete file — raw access token no longer reachable via HTTP |
| T-02-02 | Elevation of Privilege | playlists/+server.ts DELETE | mitigate | Delete file — IDOR (no userId check) eliminated by removal |
| T-02-03 | Elevation of Privilege | playlist/toggle/+server.ts PATCH | mitigate | Delete file — IDOR (no userId check) eliminated by removal |
| T-02-04 | Tampering | playlist/add/+server.ts POST | mitigate | Delete file — writes to dead table eliminated |
| T-02-05 | Denial of Service | player/start/+server.ts PUT | accept | Duplicate route; low risk but removed for hygiene |
</threat_model>

<verification>
After task completes:

```bash
# Confirm all five files gone
for f in \
  src/routes/api/spotify/token/+server.ts \
  src/routes/api/spotify/player/start/+server.ts \
  src/routes/api/spotify/playlists/+server.ts \
  src/routes/api/spotify/playlist/add/+server.ts \
  src/routes/api/spotify/playlist/toggle/+server.ts; do
  test ! -f "$f" && echo "ABSENT: $f" || echo "FAIL still exists: $f"
done

# Confirm active routes intact
for f in \
  src/routes/api/spotify/playlists/track-counts/+server.ts \
  src/routes/api/spotify/playlist/validate/+server.ts \
  src/routes/api/spotify/player/play/+server.ts; do
  test -f "$f" && echo "PRESENT: $f" || echo "FAIL missing: $f"
done

# TypeScript (email.ts errors expected — not a blocker here)
npx tsc --noEmit 2>&1 | grep 'error TS' | grep -v 'email\.ts' || echo "No new errors"
```
</verification>

<success_criteria>
- `test ! -f src/routes/api/spotify/token/+server.ts` exits 0
- `test ! -f src/routes/api/spotify/player/start/+server.ts` exits 0
- `test ! -f src/routes/api/spotify/playlists/+server.ts` exits 0
- `test ! -f src/routes/api/spotify/playlist/add/+server.ts` exits 0
- `test ! -f src/routes/api/spotify/playlist/toggle/+server.ts` exits 0
- `src/routes/api/spotify/playlists/track-counts/+server.ts` still exists
- No TypeScript errors attributable to these deletions (email.ts errors are pre-existing)
- Commit created with message: `fix(02): remove token endpoint and IDOR-vulnerable playlist routes (SEC-01, SEC-02, SEC-03)`
</success_criteria>

<output>
Create `.planning/phases/02-security-and-cleanup/02-01-SUMMARY.md` when done.
</output>
