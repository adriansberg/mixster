---
phase: 02-security-and-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/types/spotify-player.d.ts
  - src/lib/server/email.ts
  - src/lib/server/db/schema.ts
  - drizzle/
autonomous: true
requirements:
  - SEC-04
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "Web Playback SDK type file no longer exists"
    - "Dead email scaffolding no longer exists"
    - "userPlaylists table definition removed from schema.ts"
    - "Drizzle migration generated and applied — userPlaylists table dropped in DB"
    - "npx tsc --noEmit exits 0 with no errors"
  artifacts:
    - path: "src/lib/types/spotify-player.d.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/lib/server/email.ts"
      provides: "ABSENT — file must not exist"
    - path: "src/lib/server/db/schema.ts"
      provides: "schema without userPlaylists table"
      contains: "users, spotifyTokens, sessions, playedSongs"
    - path: "drizzle/"
      provides: "migration file for DROP TABLE userPlaylists"
  key_links:
    - from: "src/lib/server/db/schema.ts"
      to: "drizzle/migrations"
      via: "npx drizzle-kit generate"
      pattern: "drop.*userPlaylists|userPlaylists.*drop"
    - from: "drizzle/migrations"
      to: "PostgreSQL DB"
      via: "npx drizzle-kit migrate"
      pattern: "Applied migration"
---

<objective>
Delete two dead code files (SEC-04: Web Playback SDK types, SEC-05: email scaffolding) then remove the userPlaylists table from the Drizzle schema and apply the resulting DROP TABLE migration (SEC-06). Ends with a clean TypeScript compile.

Purpose: email.ts is the source of active TS errors (RESEND_API_KEY and EMAIL_FROM not in env schema). Its deletion plus schema cleanup brings the project to zero compile errors.

Output: Two files deleted, schema.ts cleaned, migration file created and applied, `npx tsc --noEmit` exits 0.
</objective>

<execution_context>
@/Users/asb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Drizzle config (drizzle.config.ts):
- schema: ./src/lib/server/db/schema.ts
- out: ./drizzle
- dialect: postgresql
- Migration commands: npx drizzle-kit generate / npx drizzle-kit migrate

Tables to KEEP in schema.ts: users, spotifyTokens, sessions, playedSongs
Table to REMOVE: userPlaylists (the routes that reference it were deleted in plan 01)

env.ts is already clean — no RESEND_API_KEY or EMAIL_FROM in the env schema. email.ts references
those values and causes TS errors because they are absent. Deleting email.ts resolves all TS errors.

spotify-player.d.ts declares Web Playback SDK globals (Window.Spotify, Spotify namespace).
The play page uses the Spotify Connect API (device targeting) — not the Web Playback SDK.
No file imports or references spotify-player.d.ts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead type file and email scaffolding</name>
  <files>
    src/lib/types/spotify-player.d.ts
    src/lib/server/email.ts
  </files>
  <action>
    Delete both files using git rm.

    Commands:
      git rm src/lib/types/spotify-player.d.ts
      git rm src/lib/server/email.ts

    Before deleting, confirm no file in the active codebase imports email.ts:
      grep -r "from.*email" src/ --include="*.ts" --include="*.svelte" | grep -v spotify-player

    If any importer exists, read it and determine whether it is itself dead code.
    Given the context (email.ts has no schema env vars), any importer is also dead.
    Proceed with deletion in either case — note any importers in the SUMMARY.

    Do NOT touch src/lib/server/env.ts or any other lib/server files.
  </action>
  <verify>
    <automated>
      test ! -f src/lib/types/spotify-player.d.ts &amp;&amp;
      test ! -f src/lib/server/email.ts &amp;&amp;
      echo "Both files absent — OK"
    </automated>
    <automated>
      grep -r "from.*email" src/ --include="*.ts" --include="*.svelte" 2>/dev/null | grep -v '//.*from' || echo "No active email.ts importers — OK"
    </automated>
  </verify>
  <done>
    spotify-player.d.ts and email.ts absent on disk. No active importers of email.ts remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove userPlaylists from schema and apply Drizzle migration</name>
  <files>
    src/lib/server/db/schema.ts
    drizzle/
  </files>
  <action>
    Step 1 — Read src/lib/server/db/schema.ts to locate the userPlaylists table definition.
    It is approximately lines 47-62 and includes the table declaration, column definitions,
    and any exported references (e.g., `export const userPlaylists = pgTable(...)`).
    Also check for any TypeScript type exports derived from userPlaylists (e.g., InferSelectModel).

    Step 2 — Remove the userPlaylists table block entirely. Keep: users, spotifyTokens, sessions,
    playedSongs. After editing, confirm with:
      grep -c 'userPlaylists' src/lib/server/db/schema.ts
    Result must be 0.

    Step 3 — Generate the Drizzle migration:
      npx drizzle-kit generate
    This will detect the removed table and generate a DROP TABLE migration in ./drizzle/.
    Inspect the generated SQL to confirm it drops userPlaylists and only userPlaylists.

    Step 4 — Apply the migration:
      npx drizzle-kit migrate
    DATABASE_URL must be set in the environment. If DATABASE_URL is not set, the executor
    should check .env or .env.local for the value and export it before running migrate.

    Step 5 — Verify TypeScript compiles clean:
      npx tsc --noEmit
    After email.ts deletion (task 1) and schema cleanup, this must exit 0 with no errors.
  </action>
  <verify>
    <automated>
      grep -c 'userPlaylists' src/lib/server/db/schema.ts | grep -q '^0$' &amp;&amp; echo "userPlaylists removed from schema — OK" || echo "FAIL: userPlaylists still referenced in schema.ts"
    </automated>
    <automated>
      ls drizzle/*.sql 2>/dev/null | head -5 &amp;&amp; echo "Migration file(s) present — OK"
    </automated>
    <automated>
      npx tsc --noEmit &amp;&amp; echo "TypeScript clean — OK"
    </automated>
  </verify>
  <done>
    userPlaylists removed from schema.ts (grep count = 0). Migration file generated in drizzle/.
    Migration applied successfully. npx tsc --noEmit exits 0 with no errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema→DB | Drizzle migration executes DDL against production DB — irreversible DROP TABLE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | drizzle migration DROP TABLE | mitigate | Inspect generated SQL before applying — confirm only userPlaylists is dropped |
| T-02-07 | Information Disclosure | email.ts RESEND_API_KEY | mitigate | Delete file — dead credential reference eliminated |
| T-02-08 | Denial of Service | accidental DROP of active table | mitigate | Verify migration SQL targets only userPlaylists; active tables (users, sessions, spotifyTokens, playedSongs) must not appear in DROP statement |
| T-02-SC | Tampering | npm install (drizzle-kit) | accept | drizzle-kit already in devDependencies; no new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
# Files gone
test ! -f src/lib/types/spotify-player.d.ts && echo "ABSENT: spotify-player.d.ts" || echo "FAIL"
test ! -f src/lib/server/email.ts && echo "ABSENT: email.ts" || echo "FAIL"

# Schema clean
grep -c 'userPlaylists' src/lib/server/db/schema.ts

# Active tables still defined
grep -c 'pgTable' src/lib/server/db/schema.ts  # should be 4: users, spotifyTokens, sessions, playedSongs

# Migration exists
ls drizzle/*.sql | tail -3

# TypeScript
npx tsc --noEmit && echo "CLEAN" || echo "ERRORS"
```
</verification>

<success_criteria>
- `test ! -f src/lib/types/spotify-player.d.ts` exits 0
- `test ! -f src/lib/server/email.ts` exits 0
- `grep -c 'userPlaylists' src/lib/server/db/schema.ts` returns 0
- Migration file exists in drizzle/ and targets DROP TABLE for userPlaylists only
- `npx drizzle-kit migrate` completed without error
- `npx tsc --noEmit` exits 0 — zero errors
- Commit created with message: `fix(02): delete dead types + email scaffold, drop userPlaylists from schema (SEC-04, SEC-05, SEC-06)`
</success_criteria>

<output>
Create `.planning/phases/02-security-and-cleanup/02-02-SUMMARY.md` when done.
</output>
